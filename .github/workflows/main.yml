name: Check Secret Variable

on:
  push:

jobs:
  check_variables:
    runs-on: ubuntu-latest

    steps:
    - name: Check if AZURE_STORAGE_SAS_TOKEN is set
      env:
        user1: ${{ github.actor }}
      run: |
        echo "User who triggered the action: $user1"
