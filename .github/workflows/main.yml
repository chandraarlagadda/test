name: Check Secret Variable #

on: [push]

jobs:
  check_variables:
    runs-on: ubuntu-latest

    steps:
    - name: Check if AZURE_STORAGE_SAS_TOKEN is set
      env:
        user1: ${github.event.head.user.login}
        user2:: ${github.event.base.user.login }
      run: |
        echo $user1
        echo $user2
        
        
